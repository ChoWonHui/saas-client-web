# 인수인계 — 다음 세션의 Claude 에게

이 파일은 세션이 끊긴 뒤 이어서 작업하기 위한 것이다.
**먼저 `CLAUDE.md` 를 읽고 이 파일을 읽어라.**

배포된 사이트: https://jsj32166.mycafe24.com
재배포: `.\deploy.ps1` (빌드 + 카페24 업로드). `mail.php` 는 이 스크립트가 안 올리므로 따로 올려야 한다.

---

## 1. 지금 어떤 상태인가

회사 사이트 7개 화면이 배포되어 동작 중이다.

| 경로 | 내용 |
|---|---|
| `/` | 메인. 타이핑 히어로 + 사업분야 카드 2장(IT 컨설팅 / EXPRISM) |
| `/company` | 회사소개 (소개·비전·오시는 길) |
| `/company/greeting` | 대표 인사말 + 언론 보도 3건 + 이력 |
| `/company/org` | 조직도 |
| `/biz-area/consulting` | IT 컨설팅 |
| `/design` | 디자인 시안 30종 + 색 선택 + 메뉴 선택 + 요청서 |
| `/notice`, `/contact` | 공지, 문의 |

`/design` 의 요청서는 `public/mail.php` 로 POST 해서 **jsj3216@gmail.com** 으로 메일을 보낸다.
카페24 PHP 8.4 + `mail()` 로 동작하며 이미지 첨부까지 실제 발송을 확인했다.

---

## 2. 아직 가짜 데이터인 것 (사용자 확인 필요)

**먼저 물어봐야 할 것들이다. 임의로 채우지 마라.**

| 위치 | 무엇이 가짜인가 |
|---|---|
| `company-data.js` `GREETING.lead` / `paragraphs` | 대표 인사말 본문. **내가 임의로 작성한 글**이다. 대표님 실제 표현으로 교체해야 한다 |
| `company-data.js` `CONTACT.phone` | `010-0000-0000` — 원본 사이트부터 비어 있던 값 |
| `company-data.js` `ORG` | 조직도의 본부·팀 이름. 실제 조직을 못 받아 **사업영역을 보고 내가 지어냈다** |
| `company-data.js` `NOTICES` | 공지 3건. 원본 사이트의 2023년 샘플 그대로다 |

확정된 것: 대표이사 **조성호**, 주소 **서울특별시 마포구 월드컵북로 50길 6-10**, 언론 보도 3건.

---

## 3. 손대면 안 되는 것

- **EXPRISM 표기** — KANCHENJUNGA 가 만든 제품의 브랜딩이다. 회사명으로 통일하려 들지 마라
- **회사명은 영문 `KANCHENJUNGA`** 로만 쓴다. "칸첸중가", "(주)" 를 쓰지 않는다
- **EXPRISM 주문앱 화면** (`TableOrderPage`, `StoreLandingPage`, `index.css`) — 회사 사이트와 무관하다
- `public/mail.php` 의 `MAIL_TO` — 받는 주소를 입력값으로 바꾸면 스팸 중계기가 된다

---

## 3.5. 디자인 체계 (2026-08-19 전면 재설계)

회사 사이트 7개 화면을 taste-skill 기준으로 다시 설계했다.
**새 화면을 만들 때 지켜야 할 규칙은 `CLAUDE.md` 의 "이 사이트의 디자인 체계" 표에 있다.**
요약: 왼쪽 정렬이 기본 / 카드 대신 선 / 모서리는 토큰 2개 / 닫는 띠는 `.kc-cta` 하나 / 연락 버튼은 `문의하기`.

되돌리려면 백업이 있다(이 세션 한정):
`%LOCALAPPDATA%TempclaudeD--newProject-saas-project...scratchpadackup-20260818`
git 에 올려 두는 편이 안전하다. 아래 5번 참조.

## 4. 남은 일 (우선순위 순)

1. ~~**taste-skill 하드 룰 소급 적용**~~ **완료 (2026-08-18).** 정리 내역과, 실제로는 위반이
   아니었던 항목(아이브로우)은 `CLAUDE.md` 의 "기존 화면 정리 현황" 참조.
   같이 고친 버그: 메인 하단 CTA 가 라우트 없는 `/biz-area` 를 가리켜
   `/:tenantCode` 로 떨어지고 EXPRISM 가게 랜딩이 열렸다. `/biz-area/consulting` 으로 바꿨다
2. **EXPRISM 소개 페이지** — 메인 카드에 `to` 가 없어 링크가 걸리지 않는다.
   페이지를 만들면 `company-data.js` 의 `HOME_SERVICES` 에 `to` 만 추가하면 카드가 자동으로 링크가 된다.
   사업영역 하위 메뉴가 `IT 컨설팅` 하나뿐이라 허전한 것도 같이 해결된다.
   **만들 때 `CLAUDE.md` 의 디자인 체계 표를 따를 것.** 새 레이아웃 패밀리를 하나 더 쓰면
   화면 안에서 구성이 겹치지 않는다
3. ~~**배지 언어 통일**~~ **완료.** `ALL-IN-ONE` → `올인원` 으로 한글에 맞췄다
4. **`BizAreaPage.jsx` 정리** — 메뉴·라우트에서 뺐지만 파일은 남아 있다(번들에는 안 들어감).
   산업별 솔루션(LMS/ERP/EMR)·기술 스택 내용이 아깝다면 되살리고, 아니면 지운다

---

## 5. 결정이 필요한 미해결 사항

- **git**: `.agents/` 와 `.claude/` 가 `.gitignore` 에 없다. 지금 커밋하면 스킬 13개가 저장소에 들어간다.
  팀과 규칙을 공유할 거면 그대로, 개인 설정이면 `.gitignore` 에 추가한다
- **`D:\kanchenjunga-nodejs`**: 그 프로젝트의 `public/admin/` 을 `public/mng/` 로 바꿔 놨다(참조 55곳 포함).
  원본 백업은 `public_backup_20260816`. 되돌릴지 물어봐야 한다

---

## 6. 검증 방법

빌드 통과는 "동작한다" 가 아니다. Playwright 로 실제 화면을 확인한다.
`npm run build` → `npx vite preview --port 42xx` → 스크립트로 확인 → 배포 → **배포된 주소에서 다시 확인**.

매번 보는 것: 콘솔 에러 0 / 깨진 이미지 0 / **가로 스크롤 없음(1440·785·390px)** / 모바일 메뉴 동작.
