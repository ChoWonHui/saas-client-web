# saas-client-web — 화면 개발 규칙

이 저장소는 두 가지를 서빙한다.

1. **KANCHENJUNGA 회사 사이트** — root(`/`) 와 고정 경로들 (`/company`, `/biz-area/consulting`, `/design`, `/notice`, `/contact`)
2. **EXPRISM 손님 주문앱** — QR 로 들어오는 `/{업체코드}/{테이블코드}`. 로그인 없음

> **KANCHENJUNGA 는 회사, EXPRISM 은 그 회사가 만든 제품이다.**
> 코드 곳곳의 EXPRISM 표기는 제품 브랜딩이므로 바꾸지 않는다.

배포는 카페24(`jsj32166.mycafe24.com`) 정적 호스팅. `.\deploy.ps1` 한 줄로 빌드·업로드한다.

---

## taste-skill 적용 규칙

`npx skills add https://github.com/Leonxlnx/taste-skill` 로 설치했다.
`.agents/skills/` 에 실체가 있고 `.claude/skills/` 가 심볼릭 링크로 가리킨다.

### 어디에 적용하나
**회사 사이트 화면(랜딩·소개·시안 페이지)에만** 적용한다.
EXPRISM 주문앱(`TableOrderPage`, `StoreLandingPage`)은 대상이 아니다 —
taste-skill 은 스스로 "대시보드·데이터 테이블·다단계 상품 UI 는 제외" 라고 밝히고 있다.

### 충돌하면 이 문서가 이긴다
taste-skill 은 **Tailwind v4 + Next.js + Motion** 스택을 전제로 쓰여 있다.
이 프로젝트는 **Vite + 순수 CSS** 이고, EXPRISM 규약(`../saas-admin-web/CLAUDE.md`)에
"CSS 프레임워크를 도입하지 않는다 / `:root` 토큰만 쓴다 / 기능별 접두어" 가 이미 정해져 있다.

| 항목 | 따를 것 |
|---|---|
| 스택 (Tailwind·Next·Motion 지시) | **무시** — Vite + 순수 CSS 유지 |
| 디자인 시스템 패키지 설치 지시 | **무시** — 프레임워크 도입 금지 |
| em-dash 금지, 히어로 규율, 아이브로우 제한, 레이아웃 다양성 | **따른다** — 스택과 무관한 규칙 |

### 반드시 지킬 하드 룰 (taste-skill §4.7, §9.G)
- **em-dash `—` 와 en-dash `–` 를 화면에 쓰지 않는다.** 마침표·쉼표·괄호·줄바꿈으로 바꾼다
- 히어로는 첫 화면에 다 들어간다. 제목 2줄 이하, 설명 20단어 이하, 텍스트 요소 최대 4개
- 히어로 상단 여백은 6rem 을 넘기지 않는다
- 데스크톱 내비게이션은 한 줄, 높이 80px 이하
- 아이브로우(작은 대문자 라벨)는 **섹션 수의 1/3 이하**로만 쓴다
- 같은 레이아웃 패밀리는 페이지당 한 번. 이미지+글 지그재그는 연속 2개까지
- 그리드에 빈 셀을 두지 않는다

### 작업 시작할 때
코드를 쓰기 전에 한 줄로 밝힌다:
`Reading this as: <페이지 종류> for <대상>, with a <분위기> language`
그리고 다이얼 3개를 정한다: `DESIGN_VARIANCE` / `MOTION_INTENSITY` / `VISUAL_DENSITY` (기본 8/6/4).

**이 사이트에 이미 정해진 값** (2026-08-19 전면 재설계에서 확정):
> Reading this as: corporate site for a small Korean IT consultancy, for B2B buyers and press,
> with a restrained editorial-technical language.
> `DESIGN_VARIANCE 7` / `MOTION_INTENSITY 4` / `VISUAL_DENSITY 4`

새 화면을 만들 때 이유 없이 이 값을 바꾸지 않는다.

### 이 사이트의 디자인 체계

정해 둔 규칙이라 새 화면에도 그대로 적용한다.

| 항목 | 규칙 |
|---|---|
| 축 | **왼쪽 정렬이 기본.** `SecHead` 는 왼쪽이 기본이고 `center` 를 줘야 가운데로 간다. 본문·폼·목록도 제목과 같은 세로선에서 시작한다 |
| 경계 | **카드 껍데기 대신 가로선·세로선.** 테두리 상자는 위계를 진짜로 나타낼 때만 쓴다(예: 메인의 문의 블록) |
| 모서리 | `--kc-r`(면) / `--kc-r-s`(버튼·입력칸) / `999px`(배지) **세 값만.** 새 값을 만들지 않는다 |
| 색 | 강조색은 `--kc-accent` 하나. 큰 제목에 그라데이션을 쓰지 않는다 |
| 글꼴 | 본문은 Pretendard. `KANCHENJUNGA` 워드마크만 Hanken Grotesk |
| 닫는 띠 | `.kc-cta` 하나를 모든 화면이 공유한다. 글은 왼쪽, 버튼은 오른쪽 끝(격자 배치라 JSX 는 손대지 않아도 된다) |
| 버튼 문구 | 같은 행동에는 같은 이름. 연락은 **`문의하기`** 하나로 통일했다 |

### 전면 재설계 (2026-08-19)

회사 사이트 7개 화면의 시각 언어를 다시 짰다. 정보 구조·경로·문구는 그대로 두었다(taste-skill §11.C).

무엇이 문제였나:
- 짙은 남색 스톡 사진 배너(`.kc-cta`)가 **다섯 화면에 똑같이** 들어가 있었다
- 서브 화면 상단(`.kc-sub`)도 일곱 화면 모두 같은 사무실 사진 위에 제목을 가운데로 얹었다
- `SecHead` 가 가운데 정렬이 기본이라 거의 모든 섹션이 같은 리듬으로 시작했다
- "가운데 제목 + 같은 크기 흰 카드 두 장" 이 메인·회사소개·컨설팅에 반복됐다
- 히어로는 가운데 정렬 + 회사명 그라데이션 + 우주 사진이었다

무엇을 바꿨나:
- 히어로를 **좌우 분할**로. 왼쪽 글, 오른쪽 사진. 글의 왼쪽 선을 아래 섹션 제목과 맞췄다
- 스톡 사진 배너 두 종류를 걷어내고 왼쪽 정렬로 바꿨다
- 카드 껍데기를 벗기고 가로선·세로선으로 나눴다(사업분야·비전미션·진행단계·언론보도·공지·연락처)
- 모서리 반경 5종류를 토큰 2개로 묶었다
- 본문 글꼴을 Pretendard 로. Inter 는 한글을 그리지 않아 문장 안에서 굵기가 어긋나 있었다
- 히어로 등장 지연 3.7초 → 1.4초

같이 잡은 버그:
- 모바일 드로어의 CTA 가 파란 배경에 회색 글자였다(대비 **1.65:1**, WCAG 미달).
  `.kc-drawer a` 가 `.kc-btn-primary` 보다 명시도가 높아 색을 덮어쓰고 있었다.
  `:not(.kc-btn)` 으로 뺐다. 지금 4.57:1
- 그 버튼만 문구가 `프로젝트 문의` 로 남아 있었다
- `/design` 화면이 아직 Inter 를 쓰고 있었다

### 기존 화면 정리 현황 (2026-08-18 소급 적용)

정리를 마친 것:
- `—` `–` **화면 노출 0건.** 남아 있는 것은 전부 소스 주석이라 사용자에게 보이지 않는다.
  고친 곳은 `company-data.js` 의 `HOME_SERVICES` 설명, `RequestForm.jsx` 의 입력 안내,
  `DesignPage.jsx` 의 시안 번호 자리표시자 3곳뿐이었다
- 스크롤 유도 표시 `↓` 제거 (§9.F "Scroll cues are banned"). 전용 CSS(`.kc-hero-scroll`,
  `kc-bob`, `.kc-fade`)도 같이 지웠다
- 스크롤 리스너 제거 (§5.D). `SiteShell.jsx` 의 헤더 배경 전환을 문서 최상단 감지용
  요소 + IntersectionObserver 로 바꿨다
- 배지 언어를 한글로 통일 (`ALL-IN-ONE` → `올인원`)

사실이 아니었던 것:
- **아이브로우 남용은 없다.** 실제 코드에는 아이브로우 패턴 자체가 없다.
  `ABOUT` `CONTACT` 는 내비게이션 항목이고, `NOTICE` `INQUIRY` 는 메인 하단 패널의
  제목(h3)이다. 둘 다 제목 위에 얹는 작은 대문자 라벨이 아니다

남은 것:
- 내비게이션에 `NOTICE` `CONTACT` 영문과 `회사정보` `사업영역` 한글이 섞여 있다.
  내비 문구는 taste-skill §11.F 가 "사용자 승인 없이 바꾸지 말라" 고 못박은 항목이라 그대로 뒀다
- `index.html` 은 Inter 도 계속 불러온다. **지우면 안 된다.**
  EXPRISM 주문앱(`index.css`)이 Inter 를 쓰고 있어서, 지우면 주문앱 글꼴이 바뀐다

---

## 검증

빌드 통과는 "동작한다" 가 아니다. Playwright 로 실제 화면을 확인한다.
최소한 이만큼: 콘솔 에러 0 / 깨진 이미지 0 / **가로 스크롤 없음(1440·785·390px)** / 모바일 메뉴 열고 닫기.
